<Page
    x:Class="Alfred.UI.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer VerticalScrollMode="Enabled"
                  VerticalScrollBarVisibility="Visible"
                  HorizontalScrollMode="Disabled"
                  HorizontalScrollBarVisibility="Disabled">
    <StackPanel Spacing="{x:Bind Prefs.SectionSpacing, Mode=OneWay}"
                Padding="{x:Bind Prefs.PagePadding, Mode=OneWay}">
        <TextBlock Text="Configuracion (diagnostico)"
                   FontSize="{x:Bind Prefs.PageTitleFontSize, Mode=OneWay}"
                   FontWeight="Bold" />

        <ComboBox x:Name="ThemeCombo" Header="Tema" SelectionChanged="OnThemeChanged">
            <ComboBoxItem Content="Sistema" Tag="System" />
            <ComboBoxItem Content="Claro"   Tag="Light" />
            <ComboBoxItem Content="Oscuro"  Tag="Dark" />
        </ComboBox>
        <ItemsRepeater x:Name="AccentSwatches">
            <ItemsRepeater.Layout>
                <StackLayout Orientation="Horizontal" Spacing="8" />
            </ItemsRepeater.Layout>
        </ItemsRepeater>
        <ComboBox x:Name="DensityCombo" Header="Densidad" SelectionChanged="OnDensityChanged">
            <ComboBoxItem Content="Comoda"   Tag="Comfortable" />
            <ComboBoxItem Content="Compacta" Tag="Compact" />
        </ComboBox>
        <Slider x:Name="FontSizeSlider" Minimum="11" Maximum="22" StepFrequency="1" ValueChanged="OnFontSizeChanged" />
        <TextBlock x:Name="FontSizeLabel" />
        <ToggleSwitch x:Name="AnimationsToggle" Header="Animaciones" Toggled="OnAnimationsToggled" />
        <PersonPicture x:Name="ProfilePicture" Width="48" Height="48" DisplayName="?" />
        <Button x:Name="RemovePictureButton" Content="Quitar foto" Click="OnRemoveProfilePicture" Visibility="Collapsed" />
        <Button Content="Cambiar foto" Click="OnChangeProfilePicture" />
        <TextBox x:Name="UserNameBox" Header="Nombre" />
        <TextBox x:Name="UserAgeBox" Header="Edad" />
        <TextBox x:Name="UserOccupationBox" Header="Ocupacion" />
        <TextBox x:Name="AboutUserBox" Header="Acerca de ti" />
        <Button Content="Guardar perfil" Style="{StaticResource AccentButtonStyle}" Click="OnSaveProfile" />
        <TextBox x:Name="AssistantNameBox" Header="Nombre del asistente" />
        <ComboBox x:Name="ToneCombo" Header="Tono" SelectedIndex="0">
            <ComboBoxItem Content="Profesional" Tag="professional" />
            <ComboBoxItem Content="Amigable"    Tag="friendly" />
        </ComboBox>
        <TextBox x:Name="CustomInstructionsBox" Header="Instrucciones" />
        <Button Content="Guardar personalizacion" Style="{StaticResource AccentButtonStyle}" Click="OnSavePersonalization" />
        <ToggleSwitch x:Name="EncryptionToggle" Header="Cifrado" Toggled="OnEncryptionToggled" />
        <TextBlock x:Name="EncryptionStatusText" />
        <TextBlock x:Name="EncryptionKeyText" />
        <TextBlock x:Name="GpuStatusText" Text="Detectando..." />
    </StackPanel>
    </ScrollViewer>
</Page>
