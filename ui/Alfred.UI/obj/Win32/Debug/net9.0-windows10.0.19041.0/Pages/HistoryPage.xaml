<Page
    x:Class="Alfred.UI.Pages.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Historial de preguntas" FontSize="24" FontWeight="Bold"
                   Margin="0,0,0,16" />

        <!-- Busqueda -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox x:ConnectionId='5' x:Name="SearchBox" Grid.Column="0"
                     PlaceholderText="Buscar en historial..."
                                               />
            <Button x:ConnectionId='6' Grid.Column="1" Content="Buscar"                 
                    Margin="8,0,0,0" />
        </Grid>

        <!-- Lista -->
        <ListView x:ConnectionId='2' x:Name="HistoryListView" Grid.Row="2" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            CornerRadius="8" Padding="16,12" Margin="0,4">
                        <StackPanel Spacing="6">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding Question}"
                                           FontWeight="SemiBold" FontSize="14"
                                           TextTrimming="CharacterEllipsis" MaxLines="2" />
                                <TextBlock Grid.Column="1" Text="{Binding Timestamp}" FontSize="11"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                           VerticalAlignment="Center" />
                            </Grid>
                            <TextBlock Text="{Binding Answer}" FontSize="13"
                                       TextTrimming="CharacterEllipsis" MaxLines="3"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TextBlock x:ConnectionId='3' x:Name="EmptyText" Grid.Row="2"
                   Text="Sin historial"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   Visibility="Collapsed" />
    </Grid>
</Page>

