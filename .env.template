# Alfred - Configuración de Entorno
# Este archivo es una plantilla. El instalador generará .env con valores personalizados

# === Configuración del Servidor ===
ALFRED_HOST=127.0.0.1
ALFRED_PORT=8000

# === Rutas de Documentos ===
# Ruta donde Alfred buscará tus documentos personales
# Windows: Usar barras invertidas \\ o barras normales /
# Linux/macOS: Usar barras normales /
ALFRED_DOCS_PATH=

# === Configuración de Modelos ===
# Modelo de lenguaje principal (requiere Ollama)
ALFRED_MODEL=gemma2:9b

# Modelo de embeddings para búsqueda semántica
ALFRED_EMBEDDING_MODEL=nomic-embed-text:v1.5

# === Configuración de GPU ===
# Forzar uso de CPU (true/false)
ALFRED_FORCE_CPU=false

# Dispositivo específico para usar (auto/cpu/cuda/mps)
ALFRED_DEVICE=auto

# === Configuración de Base de Datos ===
# Forzar recarga de documentos en próximo inicio
ALFRED_FORCE_RELOAD=false

# === Configuración de Logs ===
# Nivel de logging (DEBUG/INFO/WARNING/ERROR)
ALFRED_LOG_LEVEL=INFO

# Directorio para logs
ALFRED_LOG_DIR=

# === Configuración Avanzada ===
# Tamaño de chunk para documentos
ALFRED_CHUNK_SIZE=1000

# Superposición entre chunks
ALFRED_CHUNK_OVERLAP=200

# Número de documentos a recuperar por consulta
ALFRED_TOP_K=5
